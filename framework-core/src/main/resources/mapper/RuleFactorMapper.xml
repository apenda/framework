<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apenda.framework.dao.mapper.RuleFactorMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.apenda.framework.dao.entity.RuleFactor">
        <id column="id" property="id" />
        <result column="rid" property="rid" />
        <result column="factor_name" property="factorName" />
        <result column="factor" property="factor" />
        <result column="factor_method" property="factorMethod" />
        <result column="create_date" property="createDate" />
        <result column="update_date" property="updateDate" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, rid, factor_name, factor, factor_method, create_date, update_date
    </sql>

</mapper>
